package com.kemai.wremja.model.filter;

import com.kemai.util.Predicate;

/**
 * Just a marker interface to mark those predicates which match based on time.
 */
public interface TimePredicate<T> extends Predicate<T> {

}
